import "@styles/Button.css"; //dondequiera que esté la carpeta "styles", buscala y buscá button.css

const Button = () => {
  return <button className="alias">Alias import Vite</button>;
};

export default Button;
